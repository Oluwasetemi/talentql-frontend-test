name: Run Test on push and pull_request

on: [pull_request, push]

jobs:
  tests:
    name: run tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: Adzz/jest_test_action@v1.0.0
        with:
          command: yarn config set ignore-scripts false && test:all
